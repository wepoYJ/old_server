import { VerifyExceptionFilter } from './verify-exception.filter';

describe('VerifyExceptionFilter', () => {
  it('should be defined', () => {
    expect(new VerifyExceptionFilter()).toBeDefined();
  });
});
